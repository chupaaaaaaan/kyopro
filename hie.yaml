cradle:
  multi:
    - path: "./lib"
      config:
        cradle:
          cabal:
            component: "lib:kyopro"

    - path: "./template.hs"
      config:
        cradle:
          direct:
            arguments: [ "-package-env", "package.local" ]

    - path: "./sandbox"
      config:
        cradle:
          direct:
            arguments: [ "-package-env", "package.local" ]

    - path: "./atcoder.jp"
      config:
        cradle:
          direct:
            arguments: [ "-package-env", "package.local" ]
